# https://github.com/github/gitignore/blob/master/R.gitignore

# RStudio temporary files
.Rproj.user/

# R History files (can just re-run code instead)
.Rhistory
.Rapp.history

# Session Data files
.RData

# User-specific files
.Ruserdata

# Example code in package build process
*-Ex.R

# Output files from R CMD build
/*.tar.gz

# Output files from R CMD check
/*.Rcheck/

# OAuth2 token, see https://github.com/hadley/httr/releases/tag/v0.3
.httr-oauth

# Temporary files created by R markdown
*.utf8.md
*.knit.md

# knitr and R markdown default cache directories
/*_cache/
/cache/

# Folder
inst/doc

# Windows file for caching thumbnail images.
Thumbs.db

# Omit PDF output files from knitting .Rmd files in selected folders.
# This is to keep report results off of GitHub.
inst/*.pdf

# Omit EPS and TIFF files from knitting .Rmd files in selected folders.
inst/*.eps
inst/*.tiff

# Omit SPSS data files that should be downloaded from NACJD archive
# instead of distributed via GitHub.
inst/extdata/*.sav

# Omit RData files that should not be uploaded to GitHub. User can run
# script to create the data file locally.
data/*.RData

# Omit log files created by knitting to PDF.
inst/*.log
