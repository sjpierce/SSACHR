# Automatically added by RStudio (ignores the RStudio project file).
^.*\.Rproj$

# Used for temporary files.
^\.Rproj\.user$

# An Rmarkdown file used to generate README.md.
^README\.Rmd$

# Exclude PDF files created by knitting .Rmd files.
#^vignettes/*\.pdf$

# Exclude graphics folder to avoid note when checking package.
^graphics$

# Omit RData files that should not be uploaded to GitHub. User can run
# script to create the data file locally.
^data/CHR_Data.RData$

# Omit PDF output files from knitting .Rmd files in selected folders.
^inst/*\.pdf$
^inst/Drafts/*\.pdf$

# Omit text file that should not be treated as actual data.
^data/Read_This_Note\.txt$

# Omit EPS, PNG, and TIFF files from knitting .Rmd files in selected folders.
^inst/*\.eps$
^inst/*\.png$
^inst/*\.tiff$
^inst/Drafts/*\.eps$
^inst/Drafts/*\.png$
^inst/Drafts/*\.tiff$

# Omit SPSS data files that should be downloaded from NACJD archive
# instead of distributed via GitHub.
^inst/extdata/*\.sav$

# Omit log files created by knitting to PDF.
^inst/*.log

# Omit the license files.
^LICENSE\.md$
^LICENSE\.txt$
